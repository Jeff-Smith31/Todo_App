name: Frontend Deployment (deprecated)

on:
  workflow_dispatch:
  push:
    branches: [ "main" ]

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Notice
        run: |
          echo "This workflow is deprecated. The frontend is now deployed as a Docker container on the same EC2 instance as the backend."
          echo "No S3 or CloudFront actions are performed."
